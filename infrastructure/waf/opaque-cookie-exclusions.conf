# Owned by the BFF/edge boundary (ADR-0001/0016). Only the exact opaque,
# single-valued locator format is excluded from OS-filename rule 930120.
# The BFF still validates the key and server-side session. No URL, argument,
# body, cookie name, other cookie or other CRS rule is excluded.
# Review with any cookie-format or CRS-version change; not a temporary bypass.
SecRule &REQUEST_COOKIES:__Host-sajtech-session "@eq 1" \
  "id:1000010,phase:1,pass,nolog,t:none,chain"
SecRule REQUEST_COOKIES:__Host-sajtech-session "@rx \A[A-Za-z0-9_-]{1,64}\.[A-Za-z0-9_-]{43}\z" \
  "t:none,ctl:ruleRemoveTargetById=930120;REQUEST_COOKIES:__Host-sajtech-session"

SecRule &REQUEST_COOKIES:__Host-sajtech-preauth "@eq 1" \
  "id:1000011,phase:1,pass,nolog,t:none,chain"
SecRule REQUEST_COOKIES:__Host-sajtech-preauth "@rx \A[A-Za-z0-9_-]{1,64}\.[A-Za-z0-9_-]{43}\z" \
  "t:none,ctl:ruleRemoveTargetById=930120;REQUEST_COOKIES:__Host-sajtech-preauth"
