# Local Production-Fidelity Kubernetes Foundation

This directory owns the repository-defined `platform-local` kind foundation for the production-fidelity integration lane.

The lane is developer/staging integration evidence only. It must not be reported as production deployment or production readiness. Exact implementation, pins, install, verification, and rollback artifacts are completed in the same coherent change before this branch is ready for review.
