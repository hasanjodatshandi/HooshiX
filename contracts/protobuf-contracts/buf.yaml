version: v2
modules:
  - path: src/main/proto
  - path: build/buf-dependencies
    lint:
      use:
        - MINIMAL
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE

deps:
  - buf.build/bufbuild/protovalidate
