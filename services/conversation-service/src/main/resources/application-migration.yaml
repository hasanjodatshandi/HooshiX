spring:
  config:
    import: optional:configtree:/run/secrets/conversation-db-migration/
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
  main:
    web-application-type: none
management:
  endpoint:
    health:
      validate-group-membership: false
