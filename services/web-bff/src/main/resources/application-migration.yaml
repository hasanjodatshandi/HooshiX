spring:
  main:
    web-application-type: none
  config:
    import: optional:configtree:/run/secrets/web-bff-db-migration/
  flyway:
    enabled: true

server:
  port: -1

management:
  server:
    port: -1
  endpoint:
    health:
      validate-group-membership: false
