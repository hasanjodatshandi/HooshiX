apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "compromised-password-service.name" . }}
spec:
  podSelector:
    matchLabels:
      {{- include "compromised-password-service.selectorLabels" . | nindent 6 }}
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.identity.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.identity.podLabels | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.service.grpcPort }}
    {{- if .Values.observability.networkPolicy.enabled }}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ required "observability.networkPolicy.namespace is required when enabled" .Values.observability.networkPolicy.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.observability.networkPolicy.podLabels | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.service.managementPort }}
    {{- end }}
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- if .Values.observability.networkPolicy.enabled }}
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.observability.networkPolicy.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.observability.networkPolicy.podLabels | nindent 14 }}
    {{- end }}
