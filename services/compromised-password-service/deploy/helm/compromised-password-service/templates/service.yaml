apiVersion: v1
kind: Service
metadata:
  name: {{ include "compromised-password-service.name" . }}
  labels:
    {{- include "compromised-password-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "compromised-password-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
    - name: management
      port: {{ .Values.service.managementPort }}
      targetPort: management
      protocol: TCP
